# biosymphony.ai

Stealth landing page for BioSymphony — a frontier lab for cell therapeutics. A single static page: no build step, no dependencies, no external requests.

Preview locally:

```sh
python3 -m http.server 8000
```

Then open http://localhost:8000.

Deployed via Cloudflare Pages (no build command, output directory `/`).
